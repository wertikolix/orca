# Orca 0.11.2

Patch release updating the optional native Android math adapter.

## Updated

- Upgrades `orca-math-orcex` to the published Orcex `0.3.0` renderer stack.
- Preserves the corrected Maven POM publication introduced in `0.11.1`.
- Keeps the base `orca-core` and `orca-compose` artifacts free from optional math/font dependencies.
