# Orca 0.4.1

Release focus: footnotes UX stabilization.

## Highlights

- Fixed footnote backlink visibility in renderer:
  - `↩` is now shown only for the currently active footnote definition after jump.
  - normal reading flow no longer shows persistent backlink-looking elements after footnote text.
- No parser API changes.
- No breaking changes.

## Maven Coordinates

- `ru.wertik:orca-core:0.4.1`
- `ru.wertik:orca-compose:0.4.1`

## Verification

```bash
./gradlew --no-daemon --build-cache :orca-core:test :orca-compose-android:testDebugUnitTest :sample-app:assembleDebug
```
